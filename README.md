# pdm_2th

**설비 고장을 예측하는 AI 데이터 분석** 과정 학습 저장소입니다.
`pdm` = Predictive Maintenance(예측 정비).

센서 데이터로 설비의 고장 징후를 미리 읽어내는 것이 목표이고,
Python 기초 → 데이터 처리(pandas·numpy) → 시각화 → 시계열 분석 → 머신러닝 이상 탐지 순으로 진행합니다.

---

## 폴더 구조

날짜별 폴더에 그날 배운 내용을 정리합니다.

```
pdm_2th/
├── 0722/   환경 설정 · 변수 · 자료형 · 연산자 · 입력   (복습 자료 + 연습문제)
├── 0723/   문자열 다루기                              (복습 자료 + 연습문제)
├── 0724/   문자열 정리와 f-string                      (복습 자료 + 연습문제 + 종합실습)
├── 0727/   리스트 · 조건문 · 반복문                     (복습 자료 + 연습문제 + 기초 드릴)
├── 0728/   실력 다지기 응용 20제                        (처음~05_03 조합 문제)
├── 06일차/  튜플 · 셋 · 딕셔너리                          (복습 자료 + 연습문제)
├── 07일차/  함수                                        (복습 자료 + 연습문제)
├── 08일차/  모듈 · 파일 입출력                            (복습 자료 + 연습문제)
├── .gitignore
└── README.md

> 6일차부터는 폴더명을 **일차(N일차)** 로 씁니다 (장 번호 = 일차). 1~5장은 위 날짜 폴더에.
```

각 날짜 폴더에는 단원별 복습 코드와 함께 `README.md`(치트시트), `99_연습문제.py`, `99_연습문제_정답.py` 가 들어 있습니다.

---

## 진도

| 날짜 | 단원 | 내용 |
|---|---|---|
| 7/22 | 01_01 | 예측 정비와 설비 데이터 / 개발 환경(Colab·VSCode) / Git·GitHub |
| | 01_02 | `print()`, 숫자와 문자, 주석, 오류 메시지 읽기 |
| | 02_01 | 변수와 할당, 이름 규칙, 재할당 |
| | 02_02 | 자료형 `int` `float` `str` `bool`, `type()` |
| | 02_03 | 산술 · 비교 · 논리 연산자 |
| | 02_04 | `input()`, 형변환 `int()` `float()` `str()` |
| 7/23 | 03_01 | 문자열의 이해와 생성, 따옴표 규칙, 이스케이프 |
| | 03_02 | 인덱싱과 슬라이싱 |
| | 03_03 | 문자열 연산과 검색 `len` `in` `count` `find` |
| 7/24 | 03_04 | 공백과 대소문자 정리 `strip` `lower` `upper` |
| | 03_05 | 치환·분리·결합 `replace` `split` `join` |
| | 03_06 | f-string 과 텍스트 정리 `f"{value:.2f}"` |
| 7/27 | 04_01 | 리스트 — 생성·인덱싱·슬라이싱, `append` `remove` `sort` |
| | 04_02 | 조건문 — `if` `elif` `else`, 들여쓰기, 연쇄 비교 |
| | 05_01 | for 반복문 — `range`, 누적 변수 패턴, 중첩 반복 |
| | 05_02 | while 반복과 흐름 제어 — `break` `continue`, 플래그 |
| | 05_03 | 리스트 데이터 처리 — `for t in 리스트`, 필터링, `append` |

---

## 실행 방법

Python 3 설치 후, 파일이 있는 폴더에서:

```bash
python 02_01_변수.py
```

VSCode에서는 파일을 열고 **F5** 또는 오른쪽 위 **▶ 실행** 버튼.
코랩(Colab)을 쓴다면 코드를 셀에 붙여넣고 **Shift + Enter**.

복습 코드는 코드 옆에 `# →` 로 실행 결과를 적어두었습니다.
**결과를 먼저 예상한 뒤 실행해서 맞춰보는 방식**으로 사용하세요.

---

## Git 기본 명령어

```bash
git status                     # 지금 상태 확인
git add .                      # 변경사항 스테이징
git commit -m "메시지"          # 스냅샷 저장
git push                       # GitHub에 업로드
git pull                       # GitHub의 변경사항 가져오기
git log --oneline              # 커밋 기록 확인
```

커밋 메시지는 **무엇을 했는지** 드러나게 씁니다. (`수정` ❌ → `문자열 슬라이싱 예제 추가` ⭕)
